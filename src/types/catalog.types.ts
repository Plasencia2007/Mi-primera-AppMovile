export interface Product {
  id: string;
  name: string;
  description: string;
  price: string;
  rating: string;
  time: string;
  image: any;
  tag: string;
}

export interface Category {
  id: string;
  name: string;
  icon: string;
  image: any;
}
